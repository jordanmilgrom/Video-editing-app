# Cluster repeated takes

You will be given an ordered list of speech segments split by silence. Group
segments that are repeat attempts at the same line read. A cluster of one
segment is fine when the line was nailed on the first try.

Return groups via the `cluster_takes` tool.

## Segments

{{segments}}
